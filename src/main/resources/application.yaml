spring:
  application:
    name: LLMDamo
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen3-vl-rerank
      embedding:
        enabled: true

    deepseek:
      api-key: ${DEEPSEEK_API_KEY}
    ollama:

      base-url: http://localhost:11434
      chat:
        options:
          model: gemma4:e4b
          temperature: 0.7



    model:
      embedding: dashscope


  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springai?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true
    username: root
    password: abc123

